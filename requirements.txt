# ── Core ──────────────────────────────────────────────
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.11.0

# ── Machine Learning ──────────────────────────────────
scikit-learn>=1.3.0
imbalanced-learn>=0.12.0
xgboost>=2.0.0
lightgbm>=4.0.0
scikit-posthocs>=0.9.0
statsmodels>=0.14.0
joblib>=1.3.0

# ── Embeddings ────────────────────────────────────────
# Para PyTorch CPU-only (mais leve):
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
sentence-transformers>=3.0.0

# ── Datasets ──────────────────────────────────────────
datasets>=2.16.0

# ── Visualização ──────────────────────────────────────
matplotlib>=3.7.0
seaborn>=0.12.0
tqdm>=4.65.0

# ── SSL / Rede ────────────────────────────────────────
# Usa os certificados do sistema operacional (fix para redes com CA local)
truststore>=0.9.0

# ── Testes e Jupyter ──────────────────────────────────
pytest>=8.0.0
ipykernel>=6.0.0
ipywidgets>=8.0.0
jupyterlab>=4.0.0
